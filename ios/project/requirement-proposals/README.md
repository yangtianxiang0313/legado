# Requirement Proposals

这里保存 AI 从有效 Android facts 聚合出的候选需求。候选不是权威需求，不能被普通实现工作项引用，也不能由 AI 自行标记为 accepted。

去重键为 `capability + observable behavior + profile`。rejected/deferred 的裁决应保留 tombstone，避免后续扫描反复生成同一候选。
